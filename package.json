{
  "name": "cinema-library",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "pnpm -r --parallel dev",
    "dev:frontend": "pnpm --filter @cinema-library/frontend dev",
    "dev:backend": "pnpm --filter @cinema-library/backend dev",
    "build": "pnpm -r build",
    "build:cloud-function": "node scripts/build-cloud-function.mjs",
    "publish:cloud-function": "node scripts/publish-cloud-function.mjs",
    "build:frontend-artifact": "node scripts/build-frontend.mjs",
    "publish:frontend": "node scripts/publish-frontend.mjs",
    "typecheck": "pnpm -r typecheck",
    "lint": "pnpm -r lint",
    "lint:fix": "pnpm -r lint:fix",
    "fmt": "pnpm -r fmt",
    "fmt:check": "pnpm -r fmt:check",
    "check": "pnpm lint && pnpm fmt:check && pnpm typecheck",
    "fix:scripts": "oxlint scripts --fix && oxfmt scripts --write"
  },
  "devDependencies": {
    "oxfmt": "^0.48.0",
    "oxlint": "^1.63.0",
    "typescript": "^5.9.0"
  },
  "packageManager": "pnpm@10.33.2"
}
